---
import "../styles/theme.css";
import Nav from "../components/Nav.astro";

const { link, title } = Astro.props;
---

<html lang="it">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" sizes="93x93" />
  </head>
  <body class="min-h-screen bg-mang text-white">
    <Nav>
      <a
        href="/spotify/"
        class="text-sm font-bold px-3 py-2 rounded-xl bg-white bg-secondary-hover transition-colors text-black shadow"
        title="La playlist promessa"
      >
        🎹<span class="hidden sm:inline">&nbsp;&nbsp;La playlist promessa</span>
      </a>
      <a
        href="#donate"
        class="text-sm font-bold px-4 py-2 rounded-xl bg-primary bg-secondary-hover text-black shadow transition-colors"
      >
        Dona<span class="hidden sm:inline"> ora</span>
      </a>
    </Nav>

    <slot />

    <footer class="border-t border-black/10 bg-footer">
      <div class="mx-auto max-w-6xl px-4 py-10 grid md:grid-cols-2 gap-6 align-bottom">
        <div>
          <div class="flex items-center gap-3">
            <div class="size-9 rounded-xl bg-white/90 grid place-items-center" aria-hidden="true">🍉</div>
            <div class="leading-tight">
              <p class="text-sm font-bold uppercase tracking-widest">Luca Farewell Fund</p>
              <p class="text-[11px] text-white/80 -mt-0.5">Sostieni la Migrazione Assistita</p>
            </div>
          </div>
          {
            link && (
              <p class="mt-3 text-xs text-white/90 max-w-md">
                Questa pagina è una farsa. Ma se clicchi su uno dei{" "}
                <a href={link} target="_blank" class="underline decoration-dotted hover:no-underline">
                  partecipa
                </a>{" "}
                puoi fare una donazione alla Global Sumud Flotilla 🍉.
              </p>
            )
          }
        </div>
        <div class="flex md:justify-end items-end gap-4 text-xs">
          © 2025 Luca Iacobini & ChatGPT. All rights reserved.
        </div>
      </div>
    </footer>
  </body>
</html>
