---
import "../styles/theme.css";
import Nav from "../components/Nav.astro";

const { description, link, title } = Astro.props;
---

<html lang="it">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" sizes="93x93" />
  </head>
  <body class="min-h-screen bg-mang text-white font-display">
    <Nav>
      <slot name="nav" />
    </Nav>

    <slot />

    <footer class="border-t border-black/10 bg-footer">
      <div class="mx-auto max-w-6xl px-4 py-10 grid md:grid-cols-2 gap-6 align-bottom">
        <div>
          <div class="flex items-center gap-3">
            <div class="size-9 rounded-xl bg-white/90 grid place-items-center" aria-hidden="true">🍉</div>
            <div class="leading-tight">
              <p class="text-sm font-bold uppercase tracking-widest">{title}</p>
              {description && <p class="text-[11px] text-white/80 -mt-0.5">{description}</p>}
            </div>
          </div>
          {
            link && (
              <p class="mt-3 text-xs text-white/90 max-w-md">
                Questa pagina è una farsa. Ma se clicchi su uno dei{" "}
                <a href={link} target="_blank" class="underline decoration-dotted hover:no-underline">
                  partecipa
                </a>{" "}
                puoi fare una donazione alla Global Sumud Flotilla 🍉.
              </p>
            )
          }
        </div>
        <div class="flex md:justify-end items-end gap-4 text-xs">
          <span>
            © 2025 <a
              href="https://github.com/subsire/subsire.github.io/tree/mangrovia-crowdfunding"
              target="_blank"
              class="no-underline decoration-dotted hover:underline">Luca Iacobini & ChatGPT</a
            >. All rights reserved.</span
          >
        </div>
      </div>
    </footer>
  </body>
</html>
